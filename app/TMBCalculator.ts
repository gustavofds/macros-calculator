export default class TMBCalculator {
  static calculate(personData: personData): number {
    return 0;
  }
}

type personData = {
  age: number;
  sex: 'male' | 'female';
  heightInCm: number;
  weightInKg: number;
};
